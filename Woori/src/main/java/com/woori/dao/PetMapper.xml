<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woori.dao.ChatDAO">

	<insert id="newPet" parameterType="com.woori.entity.Tb_pet">
		INSERT INTO tb_pet
		(pet_name,
		pet_birthdate,
		pet_weight,
		pet_gender,
		adopted_at,
		pet_img1,
		pet_img2,
		pet_img3,
		login_email)
		VALUES
		(
		#{pet_name},
		#{pet_birthdate},
		#{pet_weight},
		#{pet_gender},
		#{adopted_at},
		#{pet_img1},
		#{pet_img2},
		#{pet_img3},
		#{login_email}
		)
		
	</insert>

	<select id="myPet" resultType="com.woori.entity.Tb_pet">
		select *
		from tb_pet
		where login_email = #{login_email}
	</select>

</mapper>

